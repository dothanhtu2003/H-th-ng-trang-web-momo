{
  "name": "momo-clone",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "npx tailwindcss -i src/styles/input.css -o public/tailwind.css --watch",
    "build": "npx tailwindcss -i src/styles/input.css -o public/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.16"
  }
}
